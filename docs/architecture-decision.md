See the API repository copy for the full decision record:

`metcc_backend/docs/architecture/00-analysis-and-migration-plan.md`

Summary: three Git remotes stay separate; Expo `app/` routes stay put; this repo adds `features/<name>/index.ts` barrels and `docs/`.
